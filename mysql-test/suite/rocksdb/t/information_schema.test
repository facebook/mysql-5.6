--source include/have_rocksdb.inc
--source include/have_log_bin.inc

select * from INFORMATION_SCHEMA.ROCKSDB_GENERIC_INFO;
select count(*) from INFORMATION_SCHEMA.ROCKSDB_GENERIC_INFO;

--disable_warnings
DROP TABLE IF EXISTS t1;
--enable_warnings

CREATE TABLE t1 (i INT, PRIMARY KEY (i) ) ENGINE = ROCKSDB;
INSERT INTO t1 VALUES (1), (2), (3);

select * from INFORMATION_SCHEMA.ROCKSDB_GENERIC_INFO;
select count(*) from INFORMATION_SCHEMA.ROCKSDB_GENERIC_INFO;

DROP TABLE t1;

