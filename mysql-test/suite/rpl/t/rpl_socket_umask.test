# -- source include/have_binlog_format_mixed_or_statement.inc
-- echo # rpl_socket_umask test starts

-- exec stat -c "%a" $MYSQL_TMP_DIR/mysqld.1.sock
-- exec stat -c "%a" $MYSQL_TMP_DIR/mysqld.2.sock

-- echo # rpl_socket_umask test ends
