# This test is to check various cases of connections
# to the admin tcpip port of ipv4

--echo ## ------------------------------------------------------------------
--echo ## -- Test of IP= 127.0.0.1 (ipv4)
--echo ## ------------------------------------------------------------------
let $IP= 127.0.0.1;
--source include/admin_connection.inc

--echo ## ------------------------------------------------------------------
--echo ## -- End of admin connection ipv4 tests
--echo ## ------------------------------------------------------------------
